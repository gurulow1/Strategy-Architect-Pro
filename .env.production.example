# Required for a production Vercel build. Set this in Vercel project settings
# to the real public Railway backend HTTPS origin (no path or trailing slash).
# Do not copy a guessed/generated Railway hostname.
VITE_API_BASE=
